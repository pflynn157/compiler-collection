
func main -> i32 is
    array numbers : i32[10];
    var x : i32 := 0;
    
    var i : i32 := 0;
    while i < 10 do
        numbers[i] := i * 2;
        i := i + 1;
    end
    
    var ret : i32 := 0;
    i := 0;
    while i < 10 do
        ret := ret + numbers[i];
        i := i + 1;
    end
    
    return 90 - ret;
end

